layouts:
  default:
  - device <- master
  - device -> worker

topologies:
  default:
    type: reqrep
    layout: default
    default-port: 20000
    ip-addresses:
      device:
      - 10.10.0.65
      - 10.10.0.55
